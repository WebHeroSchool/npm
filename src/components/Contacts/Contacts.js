import React from 'react';
import contactsStyles from './Contacts.module.css';

const Contacts = () => (
  <div className={contactsStyles.wrap}>
    <p>Check it later!</p>
  </div>
);

export default Contacts;